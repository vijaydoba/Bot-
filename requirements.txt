# Core
pandas>=2.0
numpy>=1.24

# Real market data (works wherever Yahoo Finance is reachable)
yfinance>=0.2.40

# Optional: equity-curve plotting (run_backtest.py --plot)
matplotlib>=3.7

# Optional: live trading via OANDA (only needed if you wire up OandaBroker)
# oandapyV20>=0.7

# Dev / tests
pytest>=7.0
